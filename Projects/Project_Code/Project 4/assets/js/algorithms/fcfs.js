function fcfs(jobs) {
  console.log("Starting FCFS Algorithm @ " + (
  new Date()).toString() + "!");

  standardOutput("fcfs", jobs);

  console.log("Ending FCFS Algorithm @ " + (
  new Date()).toString() + "!");
}
