function sjn(jobs) {
  console.log("Starting SJN Algorithm @ " + (
  new Date()).toString() + "!");

  standardOutput("sjn", jobs);

  console.log("Ending SJN Algorithm @ " + (
  new Date()).toString() + "!");
}
